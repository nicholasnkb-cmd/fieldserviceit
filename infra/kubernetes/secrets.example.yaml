# Example shape only. Do not apply this file or place secret values in source control.
# Provision the `app-secrets` Secret with an external secret manager, SOPS-encrypted
# manifest, or the deployment platform before applying backend-deployment.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: fieldserviceit
type: Opaque
stringData:
  DATABASE_URL: REPLACE_OUTSIDE_SOURCE_CONTROL
  JWT_SECRET: REPLACE_OUTSIDE_SOURCE_CONTROL
